apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blueapi.fullname" . }}-test-connection"
  labels:
    {{- include "blueapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blueapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
