[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
blueapi = {editable = true, extras = ["dev"], path = "."}

[packages]
blueapi = {editable = true, extras = ["rest", "client"], path = "."}

[scripts]
example = "python -m blueapi.example"
lint = "pre-commit run --all-files --show-diff-on-failure --color=always -v"
tests = "pytest"
docs = "sphinx-build -EWT --keep-going docs build/html"
# Delete any files that git ignore hides from us
gitclean = "git clean -fdX"
